fastapi==0.115.6
uvicorn[standard]==0.34.0
PyJWT==2.10.1
passlib==1.7.4
bcrypt==4.0.1  # pinned to avoid passlib "__about__" version warning
openai==1.59.7
python-dotenv==1.0.1
pydantic==2.10.4

# Optional: PyVerilog provides more accurate parsing.
# It requires a C compiler and may not have wheels for Python 3.13.
# The app falls back to a regex-based parser when PyVerilog is unavailable.
pyverilog==1.1.2
